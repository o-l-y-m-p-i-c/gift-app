/**
 * Gift Checkout Block — Checkout UI Extension (Shopify Plus only)
 *
 * This extension renders a gift block on the checkout page.
 * It shows the selected gift product with a "Free" badge.
 * Only renders on Shopify Plus stores.
 *
 * Note: @shopify/ui-extensions-react is installed during `shopify app build`
 * when the extension is deployed via Shopify CLI. The import below will
 * resolve after running `shopify app build`.
 */

// Placeholder — actual implementation will be compiled by Shopify CLI
// during the build step. This file is intentionally minimal.
export {};
